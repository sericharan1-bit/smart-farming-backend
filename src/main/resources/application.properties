spring.application.name=cabsystemsms
# spring.datasource.url=jdbc:mysql://localhost:3306/cabsystemsms
# spring.datasource.username=root
# spring.datasource.password=tiger

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true




# Use provided DB_* env vars when available, otherwise fall back to an in-memory H2 DB
spring.datasource.url=${DB_URL:jdbc:h2:mem:testdb}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=update

# Use PORT environment variable when present (Render provides $PORT)
server.port=${PORT:8080}